{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "",
  "main": "scripts.js",
  "scripts": {
    "watch": "concurrently \"npm run serve\" \"npm run babel\"",
    "babel": "babel src -d dist --preset=es2015 -ws",
    "build": "rm -rf dist && babel src -d dist --preset=es2015",
    "serve": "http-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "concurrently": "^7.3.0"
  }
}